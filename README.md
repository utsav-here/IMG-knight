# IMG-knight
Photos often end up sitting in plain, readable form in places they probably shouldn't be — synced cloud folders, shared drives, old backups. IMG-KNIGHT exists to close that gap with as little friction as possible: a single Python script that turns any image into a password-protected, tamper-evident file, and back again.
